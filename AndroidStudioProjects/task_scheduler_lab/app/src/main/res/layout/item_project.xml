<?xml version="1.0" encoding="utf-8"?>

<!--
    Project item layout.
    Энэ layout нь Home дэлгэц дээрх Projects RecyclerView дотор
    нэг project-ийг card хэлбэрээр харуулахад ашиглагдана.
-->

<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="150dp"
    android:layout_height="120dp"
    android:layout_marginEnd="12dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="#FFFFFF">

    <!-- Card доторх үндсэн контейнер -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="14dp">

        <!-- Project-ийн өнгийн indicator -->
        <View
            android:id="@+id/viewProjectColor"
            android:layout_width="6dp"
            android:layout_height="32dp"
            android:background="#F48FB1" />

        <!-- Project нэр -->
        <TextView
            android:id="@+id/tvProjectTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="Psychology of dreams"
            android:textColor="#2F2F2F"
            android:textSize="14sp"
            android:textStyle="bold" />

        <!-- Project progress (хийсэн task / нийт task) -->
        <TextView
            android:id="@+id/tvProjectProgress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="✓ 3/8 completed"
            android:textColor="#666666"
            android:textSize="12sp" />

        <!-- Project үүсгэсэн огноо -->
        <TextView
            android:id="@+id/tvProjectDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="May 15, 2020"
            android:textColor="#888888"
            android:textSize="12sp" />

    </LinearLayout>

</androidx.cardview.widget.CardView>